# MultiGoal

Fully JIT-able JAX port of the Soft Q-Learning MultiGoal point-mass environment.

Run with:

```bash
--environment.name=custom_jax.multi_goal
```

This variant is intended for `flax_full_jit` algorithms.
